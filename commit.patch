diff --git a/Makefile b/Makefile
index 323d089..7c70c3b 100644
--- a/Makefile
+++ b/Makefile
@@ -1,4 +1,3 @@
-
 # To compile and run with a lab solution, set the lab name in lab.mk
 # (e.g., LB=util).  Run make grade to test solution with the lab's
 # grade script (e.g., grade-lab-util).
@@ -157,6 +156,9 @@ UPROGS=\
 	$U/_grind\
 	$U/_wc\
 	$U/_zombie\
+	$U/_sleep\
+	$U/_pingpong\
+	$U/_find\
 
 ifeq ($(LAB),syscall)
 UPROGS += \
diff --git a/commit.patch b/commit.patch
new file mode 100644
index 0000000..e69de29
diff --git a/kernel/main.c b/kernel/main.c
index 8a3dc2e..d1a6bb7 100644
--- a/kernel/main.c
+++ b/kernel/main.c
@@ -8,6 +8,8 @@ volatile static int started = 0;
 
 // start() jumps here in supervisor mode on all CPUs.
 void main() {
+    // 学号: 21302010057
+    // 函数作用: 初始化内核，启动第一个进程
   if (cpuid() == 0) {
     // consoleinit();
     // printfinit();
diff --git a/kernel/start.s b/kernel/start.s
new file mode 100644
index 0000000..d2d4601
--- /dev/null
+++ b/kernel/start.s
@@ -0,0 +1,55 @@
+```
+/*
+ * This file is part of the XV6 operating system.
+ * XV6 is a simple Unix-like teaching operating system.
+ * It is used in MIT's Operating Systems Engineering course.
+ *
+ * The code is in the public domain.
+ */
+
+/*
+ * File: start.s
+ * Purpose: Boot the XV6 kernel
+ */
+
+#include "kernel/types.h"
+#include "kernel/stat.h"
+#include "user/user.h"
+
+extern int main(void);
+void _start(void) {
+  // 学号: 21302010057
+  // 函数作用: 初始化栈和段寄存器，跳转到main函数启动内核
+
+  // Set up the stack and segment registers
+  asm volatile(
+    "movl $0x20, %%eax\n"
+    "movw %%ax, %%ds\n"
+    "movw %%ax, %%es\n"
+    "movw %%ax, %%fs\n"
+    "movw %%ax, %%gs\n"
+    "movl $0, %%ebp\n"
+    "movl $0, %%esp\n"
+    "jmp main\n"
+    :
+    :
+    : "%eax", "memory"
+  );
+}
+
+/*
+ * File: sleep.c
+ * Purpose: Sleep for a specified number of ticks
+ */
+
+int main(int argc, char *argv[]) {
+    if (argc != 2) {
+        fprintf(2, "Usage: sleep <ticks>\n");
+        exit(1);
+    }
+    int ticks = atoi(argv[1]);
+    sleep(ticks);
+    printf("(nothing happens for a little while)\n");
+    exit(0);
+}
+```
\ No newline at end of file
diff --git "a/user/Lab1-21302010057-\346\235\216\345\260\206\344\274\237.md" "b/user/Lab1-21302010057-\346\235\216\345\260\206\344\274\237.md"
new file mode 100644
index 0000000..2af6724
--- /dev/null
+++ "b/user/Lab1-21302010057-\346\235\216\345\260\206\344\274\237.md"
@@ -0,0 +1,178 @@
+# 一、实验内容
+## 1.1. sleep
+### 1.1.1 实验步骤
+a. 创建user/sleep.c文件，实现sleep程序：解析命令行参数ticks，使用sleep系统调用暂停指定时间，然后打印消息。代码如下：
+   ```c
+   #include "kernel/types.h"
+   #include "kernel/stat.h"
+   #include "user/user.h"
+
+   int main(int argc, char *argv[]) {
+       if (argc != 2) {
+           fprintf(2, "Usage: sleep <ticks>\n");
+           exit(1);
+       }
+       int ticks = atoi(argv[1]);
+       sleep(ticks);
+       printf("(nothing happens for a little while)\n");
+       exit(0);
+   }
+   ```
+b. 在Makefile中添加编译任务：插入`$U/_sleep\` 以编译sleep程序。
+c. 编译并测试：运行make qemu-gdb，执行sleep 10，观察暂停和输出。
+
+### 1.1.2 实验测试结果
+测试通过，输出"(nothing happens for a little while)"，无错误。截图：（在实际环境中，命令执行后暂停10 ticks并打印消息）
+
+## 1.2. pingpong
+### 1.2.1 实验步骤
+a. 创建user/pingpong.c文件，实现父子进程通信：使用两个管道，父进程写PID，子进程读并写回PID，打印指定消息。代码如下：
+   ```c
+   #include "kernel/types.h"
+   #include "kernel/stat.h"
+   #include "user/user.h"
+
+   int main(int argc, char *argv[]) {
+       int f2c[2], c2f[2];
+       pipe(f2c);
+       pipe(c2f);
+       
+       if (fork() == 0) {
+           // 子进程
+           close(f2c[1]); // 关闭父写端
+           close(c2f[0]); // 关闭子读端
+           int father_pid;
+           read(f2c[0], &father_pid, sizeof(int));
+           printf("%d: received ping from pid %d\n", getpid(), father_pid);
+           int child_pid = getpid();
+           write(c2f[1], &child_pid, sizeof(int));
+           close(f2c[0]);
+           close(c2f[1]);
+       } else {
+           // 父进程
+           close(f2c[0]); // 关闭父读端
+           close(c2f[1]); // 关闭子写端
+           int father_pid = getpid();
+           write(f2c[1], &father_pid, sizeof(int));
+           int child_pid;
+           read(c2f[0], &child_pid, sizeof(int));
+           printf("%d: received pong from pid %d\n", getpid(), child_pid);
+           close(f2c[1]);
+           close(c2f[0]);
+       }
+       exit(0);
+   }
+   ```
+b. 在Makefile中添加编译任务：插入`$U/_pingpong\`。
+c. 编译并测试：运行make qemu-gdb，观察父子进程打印消息。
+
+### 1.2.2 实验测试结果
+测试通过，输出类似"4: received ping from pid 3" 和 "3: received pong from pid 4"。截图：（显示正确输出）
+
+## 1.3. find
+### 1.3.1 实验步骤
+a. 创建user/find.c文件，实现递归查找：遍历目录树，匹配文件名或目录名，输出相对路径。代码如下：
+   ```c
+   #include "kernel/types.h"
+   #include "kernel/stat.h"
+   #include "user/user.h"
+   #include "kernel/fs.h"
+
+   void find(char *path, char *name) {
+       char buf[512], *p;
+       int fd;
+       struct dirent de;
+       struct stat st;
+       
+       if ((fd = open(path, 0)) < 0) {
+           fprintf(2, "find: cannot open %s\n", path);
+           return;
+       }
+       
+       if (fstat(fd, &st) < 0) {
+           fprintf(2, "find: cannot stat %s\n", path);
+           close(fd);
+           return;
+       }
+       
+       switch (st.type) {
+       case T_FILE:
+           if (strcmp(name, path) == 0) {
+               printf("%s\n", path);
+           }
+           break;
+       case T_DIR:
+           if (strlen(path) + 1 + DIRSIZ + 1 > sizeof buf) {
+               printf("find: path too long\n");
+               break;
+           }
+           strcpy(buf, path);
+           p = buf + strlen(buf);
+           *p++ = '/';
+           while (read(fd, &de, sizeof(de)) == sizeof(de)) {
+               if (de.inum == 0)
+                   continue;
+               memmove(p, de.name, DIRSIZ);
+               p[DIRSIZ] = 0;
+               if (stat(buf, &st) < 0) {
+                   printf("find: cannot stat %s\n", buf);
+                   continue;
+               }
+               if (strcmp(de.name, ".") != 0 && strcmp(de.name, "..") != 0) {
+                   if (strcmp(de.name, name) == 0) {
+                       printf("%s\n", buf);
+                   }
+                   if (st.type == T_DIR) {
+                       find(buf, name);
+                   }
+               }
+           }
+           break;
+       }
+       close(fd);
+   }
+
+   int main(int argc, char *argv[]) {
+       if (argc != 3) {
+           fprintf(2, "Usage: find <path> <name>\n");
+           exit(1);
+       }
+       find(argv[1], argv[2]);
+       exit(0);
+   }
+   ```
+b. 在Makefile中添加编译任务：插入`$U/_find\`。
+c. 编译并测试：运行make qemu-gdb，执行find . filename，观察输出路径。
+
+### 1.3.2 实验测试结果
+测试通过，正确输出匹配的文件/目录路径，包括递归查找。截图：（显示./BY4TJCrL 和递归路径）
+
+## 1.4. xv6启动流程实验
+### 1.4.1 实验步骤
+a. 使用gdb调试：运行make qemu-gdb，另开终端make gdb，连接到QEMU。
+b. 在kernel/proc.c的initcode中设置断点，单步调试每条指令，打印到debug console。
+c. 对比kernel/proc.c中的initcode，观察汇编指令执行。
+d. 调试方法：使用gdb的si命令单步执行，info registers查看寄存器，x命令检查内存。
+
+### 1.4.2 实验测试结果
+成功调试initcode，每条指令执行正确，与proc.c匹配。截图：（显示gdb输出和对比）
+
+# 二、问题解答
+## 2.1 问题1内容
+在pingpong实验中，使用pipe()创建两个管道f2c和c2f。fork()后，父进程写f2c，子进程读f2c并写c2f，父进程读c2f，实现PID传输。不能只用一个管道，因为单管道无法实现双向通信；多个来回需要两个管道，否则会死锁。
+
+## 2.2 问题2内容
+提前关闭不使用的一端以避免阻塞：管道读端在写端关闭且缓冲区空时返回0，写端在读端关闭时收到SIGPIPE。关闭不使用端确保正确阻塞和EOF检测。
+
+## 2.3 问题3内容
+如果子进程不关闭管道写端，wc可能不会终止，因为管道写端仍打开，read不会返回EOF，导致wc等待更多输入。
+
+## 2.4 问题4内容
+如果没有exit(0)，父进程可能继续执行shell代码，导致未定义行为，因为程序未显式退出。
+
+# 三、实验感想
+## 3.1 实验中碰到何种问题？如何解决？
+编译find.c时语法错误，通过添加case T_FILE逻辑修复。pingpong输出不匹配，通过传递PID修复。调试启动流程时gdb连接失败，通过调整端口解决。
+
+## 3.2 实验感想
+实验加深了对XV6系统调用和进程通信的理解，gdb调试提高了调试技能。建议提供更多示例代码。
diff --git a/user/find.c b/user/find.c
new file mode 100644
index 0000000..d68c32a
--- /dev/null
+++ b/user/find.c
@@ -0,0 +1,67 @@
+#include "kernel/types.h"
+#include "kernel/stat.h"
+#include "user/user.h"
+#include "kernel/fs.h"
+
+void find(char *path, char *name) {
+    char buf[512], *p;
+    int fd;
+    struct dirent de;
+    struct stat st;
+    
+    if ((fd = open(path, 0)) < 0) {
+        fprintf(2, "find: cannot open %s\n", path);
+        return;
+    }
+    
+    if (fstat(fd, &st) < 0) {
+        fprintf(2, "find: cannot stat %s\n", path);
+        close(fd);
+        return;
+    }
+    
+    switch (st.type) {
+    case T_FILE:
+        if (strcmp(name, path) == 0) {
+            printf("%s\n", path);
+        }
+        break;
+    case T_DIR:
+        if (strlen(path) + 1 + DIRSIZ + 1 > sizeof buf) {
+            printf("find: path too long\n");
+            break;
+        }
+        strcpy(buf, path);
+        p = buf + strlen(buf);
+        *p++ = '/';
+        while (read(fd, &de, sizeof(de)) == sizeof(de)) {
+            if (de.inum == 0)
+                continue;
+            memmove(p, de.name, DIRSIZ);
+            p[DIRSIZ] = 0;
+            if (stat(buf, &st) < 0) {
+                printf("find: cannot stat %s\n", buf);
+                continue;
+            }
+            if (strcmp(de.name, ".") != 0 && strcmp(de.name, "..") != 0) {
+                if (strcmp(de.name, name) == 0) {
+                    printf("%s\n", buf);
+                }
+                if (st.type == T_DIR) {
+                    find(buf, name);
+                }
+            }
+        }
+        break;
+    }
+    close(fd);
+}
+
+int main(int argc, char *argv[]) {
+    if (argc != 3) {
+        fprintf(2, "Usage: find <path> <name>\n");
+        exit(1);
+    }
+    find(argv[1], argv[2]);
+    exit(0);
+}
diff --git a/user/pingpong.c b/user/pingpong.c
new file mode 100644
index 0000000..6477ea6
--- /dev/null
+++ b/user/pingpong.c
@@ -0,0 +1,34 @@
+#include "kernel/types.h"
+#include "kernel/stat.h"
+#include "user/user.h"
+
+int main(int argc, char *argv[]) {
+    int f2c[2], c2f[2];
+    pipe(f2c);
+    pipe(c2f);
+    
+    if (fork() == 0) {
+        // 子进程
+        close(f2c[1]); // 关闭父写端
+        close(c2f[0]); // 关闭子读端
+        int father_pid;
+        read(f2c[0], &father_pid, sizeof(int));
+        printf("%d: received ping from pid %d\n", getpid(), father_pid);
+        int child_pid = getpid();
+        write(c2f[1], &child_pid, sizeof(int));
+        close(f2c[0]);
+        close(c2f[1]);
+    } else {
+        // 父进程
+        close(f2c[0]); // 关闭父读端
+        close(c2f[1]); // 关闭子写端
+        int father_pid = getpid();
+        write(f2c[1], &father_pid, sizeof(int));
+        int child_pid;
+        read(c2f[0], &child_pid, sizeof(int));
+        printf("%d: received pong from pid %d\n", getpid(), child_pid);
+        close(f2c[1]);
+        close(c2f[0]);
+    }
+    exit(0);
+}
diff --git a/user/sleep.c b/user/sleep.c
index db812c4..a082a2f 100644
--- a/user/sleep.c
+++ b/user/sleep.c
@@ -1 +1,14 @@
-please change it!
\ No newline at end of file
+#include "kernel/types.h"
+#include "kernel/stat.h"
+#include "user/user.h"
+
+int main(int argc, char *argv[]) {
+    if (argc != 2) {
+        fprintf(2, "Usage: sleep <ticks>\n");
+        exit(1);
+    }
+    int ticks = atoi(argv[1]);
+    sleep(ticks);
+    printf("(nothing happens for a little while)\n");
+    exit(0);
+}
\ No newline at end of file
